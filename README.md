- 单 GPU
  ```
  sudo apt update && sudo apt -y install wget && sudo wget https://github.com/xu574303479/xen-miner-sh/blob/master/xen-miner-1.sh && sudo chmod +x xen-miner-1.sh && sudo ./xen-miner-1.sh
  ```

- 多 GPU
  ```
  sudo apt update && sudo apt -y install wget && sudo wget https://github.com/xu574303479/xen-miner-sh/blob/master/xen-miner-2.sh && sudo chmod +x xen-miner-2.sh && sudo ./xen-miner-2.sh
  ```
